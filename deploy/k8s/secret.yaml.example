# ⚠️ Aspirational — not applied. See deploy/k8s/README.md.
#
# COPY THIS FILE TO secret.yaml AND DO NOT COMMIT IT.
#
# Generate a real SECRET_KEY and base64-encode:
#   python3 -c 'import secrets; print(secrets.token_hex(32))'
#   printf '%s' '<the-hex-above>' | base64 -w0
#
# Real deployments should NOT ship secrets as plain K8s Secret objects —
# use sealed-secrets, external-secrets pointing at a KMS, or Vault.
# This file exists purely to describe the shape of what the deployment
# expects.

apiVersion: v1
kind: Secret
metadata:
  name: foi-tracker
type: Opaque
data:
  secret-key: PGJhc2U2NC1lbmNvZGVkLXNlY3JldC1oZXJlPg==  # replace this stub
