# ⚠️ Aspirational — not applied. See deploy/k8s/README.md.
apiVersion: v1
kind: Service
metadata:
  name: foi-tracker
  labels:
    app: foi-tracker
spec:
  type: ClusterIP
  selector:
    app: foi-tracker
  ports:
    - name: http
      port: 80
      targetPort: http

# Real deployment would add an Ingress here (nginx-ingress or similar)
# with TLS from cert-manager. Not included — depends on the target cluster.
